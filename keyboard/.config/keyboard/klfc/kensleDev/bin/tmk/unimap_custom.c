// Generated by KLFC 1.5.6
// https://github.com/39aldo39/klfc

#include "unimap_trans.h"
#include "action_util.h"
#include "action_layer.h"

enum function_id {
    F_MODIFIER,
};

enum macro_id {
    LIG__exclam_,
    LIG__at_,
    LIG__numbersign_,
    LIG__underscore_,
    LIG__percent_,
    LIG__question_,
    LIG__quotedbl_,
    LIG__asciicircum_,
    LIG__dollar_,
    LIG__bar_,
    LIG__braceleft_,
    LIG__parenleft_,
    LIG__ampersand_,
    LIG__parenright_,
    LIG__braceright_,
};

enum modifier_id {
    MOD_EXTEND,
};

#define AC_FN0 ACTION_FUNCTION_OPT(F_MODIFIER, MOD_EXTEND)
#define AC_FN1 ACTION_MACRO(LIG__exclam_)
#define AC_FN2 ACTION_MACRO(LIG__at_)
#define AC_FN3 ACTION_MACRO(LIG__numbersign_)
#define AC_FN4 ACTION_MACRO(LIG__underscore_)
#define AC_FN5 ACTION_MACRO(LIG__percent_)
#define AC_FN6 ACTION_MACRO(LIG__question_)
#define AC_FN7 ACTION_MACRO(LIG__quotedbl_)
#define AC_FN8 ACTION_MACRO(LIG__asciicircum_)
#define AC_FN9 ACTION_MACRO(LIG__dollar_)
#define AC_FN10 ACTION_MACRO(LIG__bar_)
#define AC_FN11 ACTION_MACRO(LIG__braceleft_)
#define AC_FN12 ACTION_MACRO(LIG__parenleft_)
#define AC_FN13 ACTION_MACRO(LIG__ampersand_)
#define AC_FN14 ACTION_MACRO(LIG__parenright_)
#define AC_FN15 ACTION_MACRO(LIG__braceright_)

#ifdef KEYMAP_SECTION_ENABLE
const action_t actionmaps[][UNIMAP_ROWS][UNIMAP_COLS] __attribute__ ((section (".keymap.keymaps"))) = {
#else
const action_t actionmaps[][UNIMAP_ROWS][UNIMAP_COLS] PROGMEM = {
#endif
    // None
    [0] = UNIMAP(
               F13, F14, F15, F16, F17, F18, F19, F20, F21, F22, F23, F24,
     ESC,       F1,  F2,  F3,  F4,  F5,  F6,  F7,  F8,  F9, F10, F11, F12,          PSCR,SLCK,PAUS,         VOLD,VOLU,MUTE,
     GRV,   1,   2,   3,   4,   5,   6,   7,   8,   9,   0,MINS, EQL,JYEN,BSPC,      INS,HOME,PGUP,    NLCK,PSLS,PAST,PMNS,
     TAB,   Q,   W,   E,   R,   T,   Y,   U,   I,   O,   P,LBRC,RBRC,     BSLS,      DEL, END,PGDN,      P7,  P8,  P9,PPLS,
     FN0,   A,   S,   D,   F,   G,   H,   J,   K,   L,SCLN,QUOT,     NUHS, ENT,                          P4,  P5,  P6,PCMM,
    LSFT,NUBS,   Z,   X,   C,   V,   B,   N,   M,COMM, DOT,SLSH,       RO,RSFT,            UP,           P1,  P2,  P3,PENT,
    LCTL,LGUI,NLCK,MHEN,           SPC,          HENK,KANA,RALT,RGUI, APP,RCTL,     LEFT,DOWN,RGHT,      P0,     PDOT,PEQL
    ),
    // Extend
    [1] = UNIMAP(
              TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,
    TRNS,     TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,          TRNS,TRNS,TRNS,         TRNS,TRNS,TRNS,
    TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,     TRNS,TRNS,TRNS,    TRNS,TRNS,TRNS,TRNS,
    TRNS, FN1, FN2, FN3, FN4, FN5, FN5,HOME,PGDN,PGUP, END,TRNS,TRNS,     TRNS,     TRNS,TRNS,TRNS,    TRNS,TRNS,TRNS,TRNS,
    TRNS, FN6, GRV, FN7,QUOT, FN8, FN9,LEFT,DOWN,  UP,RGHT,TRNS,     TRNS,TRNS,                        TRNS,TRNS,TRNS,TRNS,
    TRNS,TRNS,FN10,LBRC,FN11,FN12,FN13,BSLS,FN14,FN15,RBRC,FN10,     TRNS,TRNS,          TRNS,         TRNS,TRNS,TRNS,TRNS,
    TRNS,TRNS,TRNS,TRNS,          TRNS,          TRNS,TRNS,TRNS,TRNS,TRNS,TRNS,     TRNS,TRNS,TRNS,    TRNS,     TRNS,TRNS
    ),
};

const macro_t *action_get_macro(keyrecord_t *record, uint8_t id, uint8_t opt) {
    switch (id) {
        case LIG__exclam_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(1), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__at_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(2), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__numbersign_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(3), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__underscore_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(MINS), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__percent_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(5), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__question_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(SLSH), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__quotedbl_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(QUOT), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__asciicircum_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(6), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__dollar_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(4), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__bar_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(BSLS), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__braceleft_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(LBRC), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__parenleft_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(9), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__ampersand_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(7), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__parenright_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(0), U(LSFT), RM(), END) :
                   MACRO_NONE;
        case LIG__braceright_:
            return record->event.pressed ?
                   MACRO(SM(), CM(), D(LSFT), T(RBRC), U(LSFT), RM(), END) :
                   MACRO_NONE;
    }
    return MACRO_NONE;
}

enum vmod_mask {
    VMOD_EXTEND_MASK = 1,
};

uint8_t vmods = 0;

const uint8_t layer_states[] = {
    0x1, // None
    0x3, // Extend
};

void action_function(keyrecord_t *record, uint8_t id, uint8_t opt) {
    uint8_t pressed = record->event.pressed;
    switch (id) {
        case F_MODIFIER:
            // Set the new modifier
            switch (opt) {
                case MOD_EXTEND: pressed ? (vmods |= VMOD_EXTEND_MASK) : (vmods &= ~VMOD_EXTEND_MASK); break;
            }

            // Update the layer
            uint8_t mods = get_mods();
            uint8_t layer_index = 0;
            layer_index |= vmods << 0;
            layer_clear();
            layer_or(layer_states[layer_index]);
            break;
    }
}
